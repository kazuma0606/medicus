[project]
name = "medicus-visualization"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.8",
    "numpy>=1.26",
    "japanize-matplotlib>=1.1.3",
    "scipy>=1.12",
    "setuptools>=70",
]

[tool.uv]
dev-dependencies = []
