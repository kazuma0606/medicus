{-# LANGUAGE OverloadedStrings #-}

module GraphQL.SchemaSpec (spec) where

import Test.Hspec

spec :: Spec
spec = do
    describe "GraphQL Schema" $ do
        it "placeholder test" $ do
            -- TODO: Implement GraphQL schema tests
            True `shouldBe` True
